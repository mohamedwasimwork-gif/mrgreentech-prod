# Production env for the frontend — read at build time by Next.js.
# Cloudflare Pages also lets you set this in the dashboard, which
# overrides this file. Keep this file as the source of truth in the repo.

# Point the public site at the Render-hosted FastAPI backend.
NEXT_PUBLIC_API_BASE=https://api.mrgreentech.in
